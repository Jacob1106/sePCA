% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{plot_pca}
\alias{plot_pca}
\title{Create a PCA scatter plot}
\usage{
plot_pca(
  pca_result,
  color_by = NULL,
  shape_by = NULL,
  pcs = c(1, 2),
  point_size = 4
)
}
\arguments{
\item{pca_result}{A list containing PCA scores in the \code{scores}
element, as returned by \code{\link{run_pca}}.}

\item{color_by}{A \code{character} scalar naming a column in
\code{pca_result$scores} to color points by. If \code{NULL}, uses default
black points.}

\item{shape_by}{A \code{character} scalar naming a column in
\code{pca_result$scores} to set point shapes by. If \code{NULL}, uses
circles.}

\item{pcs}{A \code{numeric} vector of length 2 giving the principal
components to plot on x and y axes (default: \code{c(1, 2)}).}

\item{point_size}{A \code{numeric} scalar giving point size in ggplot units
(default: \code{4}).}
}
\value{
A \code{ggplot} object.
}
\description{
Generates a ggplot2 scatter plot of PCA scores with optional coloring
and shape aesthetics.
}
\examples{
# res <- run_pca(example_se, color_by = "treatment")
# p <- plot_pca(res, color_by = "treatment", shape_by = "batch")
# print(p)

}
